node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.env.local
.vscode
.idea

# Office temp/lock files
~$*
*.tmp

# Data mentah / file lokal (gak perlu di repo)
*.xlsx
/npm

# Cloudflare Wrangler cache (R2/Pages CLI)
.wrangler/

# Artefak non-app (presentasi budget, dsb) — bukan bagian template
*.pptx
*.docx
/scripts/

# Backup tooling — config berisi password & hasil dump (JANGAN ke repo)
ops/backup/supabase-backup.config.ps1
ops/backup/dumps/
ops/backup/backup.log
